# Runtime dependencies only — dev tools live in pyproject.toml ([project.optional-dependencies] dev).
# For development, install everything with: pip install -e ".[dev]"
requests>=2.33.1
pydantic>=2.13.4
httpx>=0.28.1
typing-extensions>=4.15.0
